package com.ticktock.controller;

import javafx.fxml.FXML;

public class MainController {
    /*
    Handles user interactions with the main UI (handlers for UI elements)
    Main UI logic
    Calls SessionService for data
     */
}
